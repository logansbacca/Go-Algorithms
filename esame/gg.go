package main

import (
	"fmt"
	"strings"
)



func main() {
	var word string
	var letter string
	var counter int

	if strings.Contains(word, letter) {
		counter++
	}
	fmt.Print(counter)
}
